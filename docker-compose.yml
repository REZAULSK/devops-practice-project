version: '3.8'

services:
  backend-server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - MONGODB_ATLAS_CONNECTION=mongodb+srv://rezaulkarim869_db_user:RsfmQMKOFQ0KGmDC@cluster0.cblk8ih.mongodb.net/?appName=Cluster0

  frontend-client:
    build: ./client
    ports:
      - "3000:3000" # 👈 এখানে পোর্ট ৩০০০ সেট করা হয়েছে
    environment:
      - VITE_API_BASE_URL=http://localhost:5000/api
    depends_on:
      - backend-server
